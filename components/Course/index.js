import React, { Component } from 'react';
import { View, Text } from 'react-native';
import {InputElement ,Input} from "@ui-kitten/components"

export default class Course extends Component {
  render() {
    return (
      <View>
         <Input />
      </View>
    );
  }
}
